var article_data = [{
	"id": 1,
	"subject": "Accessing the component from the outside",
	"memo": "You don’t always have the luxury of starting a brand new React app from scratch. Some‐ times you need to hook into an existing application or a website and migrate to React one piece at a time. React was designed to work with any pre-existing codebase you might have. After all, the original creators of React couldn’t stop the world are rewrite an entire huge application (Facebook) completely from scratch."
}];

var EVENT_KEYS = {
	'DATA_CHANGE': 'DATA CHANGE',
	'LIST_CLICK': 'LIST CLICK'
};

module.exports = {article_data: article_data, EVENT_KEYS: EVENT_KEYS};